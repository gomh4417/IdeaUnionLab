rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 중에는 모든 읽기/쓰기 허용 (나중에 수정 필요)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
